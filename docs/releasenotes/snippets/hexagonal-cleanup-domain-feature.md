* Renamed `AppTrackData.kt` to `AppTrack.kt` to match the class name.
* Extracted `OutboxViewerPartition` to its own model file.
* Moved `NumberFormatting.kt` to a dedicated `util` package.
* Standardized time types: `java.time.Instant` replaced with `kotlin.time.Instant` in domain-api.
