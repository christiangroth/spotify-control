* Fixed a crash on the dashboard page when running in native mode.
* Added an automated check that renders the dashboard page against the packaged native build, so this kind of issue is caught before deployment.
