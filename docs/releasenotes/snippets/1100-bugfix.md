* Fixed the migration to Shallow Artists incorrectly flagging already-synced artists as awaiting a sync decision instead of keeping their existing sync/shallow state.
* Confirming an artist's sync status on the Artist Settings or Catalog page no longer triggers a full rebuild of all playback statistics, only the periods that actually include that artist's plays.
